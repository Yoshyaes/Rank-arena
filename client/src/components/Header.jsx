/**
 * Old standalone header is replaced by the TAG site header (provided by WordPress).
 * App.jsx renders an in-page tab strip; this component is now a no-op so any
 * leftover imports keep compiling without rendering a duplicate nav.
 */
export default function Header() {
  return null;
}
